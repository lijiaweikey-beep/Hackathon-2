# 番外关卡锁定设计

## 目标

人生之外区域里的番外关卡默认折叠并锁定；只有所有人生主线关卡完成后，番外区才展开并允许进入。

## 方案

- 复用 `storyProgress.isComplete()` 作为番外区总锁，不新增存档字段。
- 未完成全部主线时，事件轴只渲染主线卡片，保留「人生之外」分隔带并显示锁定状态。
- 完成全部主线后，事件轴继续渲染番外卡片，番外按原有入口逻辑进入。

## 验证

- 单测覆盖主线未全通时番外不渲染。
- 单测覆盖主线全通后番外渲染并可进入。
