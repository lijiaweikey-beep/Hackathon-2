# 超市取证完整玩法重构实施计划

> **面向执行者：** 逐项执行，每个行为先写失败测试，再写最小实现。

**目标：** 统一超市关卡的角色行为并完成真实、连贯、可反馈的拍照取证流程。

**架构：** 独立体验组合现有共享角色系统；世界层提供统一障碍与空间查询；目标
事件和拍照规则保持为关卡内部的小型模块。

**技术：** 原生 JavaScript、Three.js、CSS、Node.js 内置测试运行器。

## 全局约束

- 不新增第三方依赖。
- 不修改共享角色移动参数。
- 不处理本轮范围外的横屏布局。
- 不纳入用户已有的 `package-lock.json` 改动。

## 任务一：真实取证规则

**文件：**

- 修改 `src/levels/supermarket/rules.js`
- 修改 `tests/levels/supermarketRules.test.js`

**步骤：**

- [ ] 增加双方入镜、唯一事件和失败原因的失败测试。
- [ ] 确认测试因现有规则缺失而失败。
- [ ] 最小实现取景条件、事件去重和原因返回。
- [ ] 运行规则测试确认通过。

## 任务二：统一角色与空间碰撞

**文件：**

- 修改 `src/levels/supermarket/actors.js`
- 修改 `src/levels/supermarket/world.js`
- 修改 `tests/levels/supermarketActors.test.js`
- 修改 `tests/levels/supermarketWorld.test.js`

**步骤：**

- [ ] 增加标准角色状态、柜台碰撞、遮挡和真实出口的失败测试。
- [ ] 确认测试因当前静态模型和不完整空间数据而失败。
- [ ] 使用共享角色工厂创建阵容，并暴露统一空间查询。
- [ ] 运行人物和世界测试确认通过。

## 任务三：连贯目标事件

**文件：**

- 新增 `src/levels/supermarket/targetSequence.js`
- 新增 `tests/levels/supermarketTargetSequence.test.js`

**步骤：**

- [ ] 增加分散、靠近、互动和离开状态转换的失败测试。
- [ ] 确认测试因模块尚不存在而失败。
- [ ] 使用注入的共享导航函数实现最小状态机。
- [ ] 运行状态机测试确认通过。

## 任务四：体验组合与反馈

**文件：**

- 修改 `src/levels/supermarket/createExperience.js`
- 修改 `src/levels/supermarket/styles.css`
- 修改 `tests/levels/supermarketLevel.test.js`

**步骤：**

- [ ] 增加共享移动、警戒说明、拍照反馈和出口流程的失败测试。
- [ ] 确认测试因现有独立坐标循环而失败。
- [ ] 组合共享角色系统、目标状态机与新规则。
- [ ] 实现对焦、快门、证据卡和失败原因反馈。
- [ ] 运行超市关卡测试确认通过。

## 任务五：完整验证与提交

- [ ] 运行全量自动化测试。
- [ ] 运行生产构建。
- [ ] 在浏览器中验证移动、碰撞、人群、目标循环、拍照、警戒和出口。
- [ ] 仅暂存本任务文件并使用中文提交信息提交。
