# 超市取证玩法统一修订实施计划

> **执行要求：** 必须逐项按测试先行执行本计划，并在每个阶段验证共享关卡没有回归。

**目标：** 让超市取证复用第一关的输入与界面，只保留记忆情侣、跟随和拍摄四张照片的核心玩法。

**架构：** 独立关卡继续拥有自己的世界、目标状态机和拍照规则，但共享输入控制器与全局界面。超市只提供界面状态，不再复制任务栏、状态栏、摇杆和暂停按钮。

**技术栈：** 原生 JavaScript、Three.js、Vite、Node.js 测试运行器。

## 全局约束

- 不增加依赖，不修改其他关卡业务规则。
- 第一关与超市使用同一输入控制器和同一界面结构。
- 开局不标记主角，情侣只标记三秒。
- 情侣间距保持 0.8～1.5 个世界单位。
- 第四张有效照片立即通关。
- 删除警戒、撤离、顶部白色灯具、悬浮灯牌和红色出口条。
- 不处理手机横屏适配。

---

### 任务一：共享输入支持独立关卡清理

**文件：**
- 修改：`src/systems/createInputController.js`
- 测试：`tests/systems/inputController.test.js`

**接口：**
- 输入：可选的 `listen(target, type, handler, options)`。
- 输出：现有 `readDirection`、`applyReverseLock`、`consumeAction`、`reset`、`bind` 保持不变。

- [ ] 增加失败测试，证明注入的监听函数接管全部事件登记。
- [ ] 运行该测试并确认因尚未支持注入而失败。
- [ ] 让输入控制器统一通过注入监听函数登记事件，默认仍调用原生监听。
- [ ] 运行输入控制器测试并确认通过。

### 任务二：情侣结伴与四张即通关

**文件：**
- 修改：`src/levels/supermarket/targetSequence.js`
- 修改：`src/levels/supermarket/rules.js`
- 测试：`tests/levels/supermarketTargetSequence.test.js`
- 测试：`tests/levels/supermarketRules.test.js`

**接口：**
- `createSupermarketTargetSequence(...).snapshot()` 增加 `introMarkerVisible`。
- `createPhotoEvidenceRules(...).capture()` 在第四张返回 `won: true`。

- [ ] 增加失败测试，证明标记三秒后永久消失且情侣不会远距离分散。
- [ ] 增加失败测试，证明第四张有效照片直接成功且不需要出口。
- [ ] 运行定向测试并确认按预期失败。
- [ ] 用同一互动点的左右偏移驱动情侣结伴移动，删除分散目的地。
- [ ] 删除出口状态和到达出口规则，第四张照片直接写入成功状态。
- [ ] 运行两组定向测试并确认通过。

### 任务三：复用第一关界面和输入

**文件：**
- 修改：`src/runtime/createExperienceHost.js`
- 修改：`src/runtime/createGameApplication.js`
- 修改：`src/ui/createGameUiController.js`
- 修改：`src/levels/supermarket/createExperience.js`
- 修改：`src/levels/supermarket/styles.css`
- 修改：`src/styles.css`
- 测试：`tests/runtime/experienceHost.test.js`
- 测试：`tests/levels/supermarketExperience.test.js`

**接口：**
- 独立体验主机向超市暴露共享控制器和共享界面状态更新函数。
- 超市体验声明复用共享界面，状态包含任务文字、倒计时、照片进度、线索和相机图标。

- [ ] 增加失败测试，证明超市不再输出自定义顶部栏、警戒、出口和独立摇杆。
- [ ] 增加失败测试，证明共享界面显示超市倒计时、照片进度和相机按钮。
- [ ] 运行定向测试并确认按预期失败。
- [ ] 将共享输入和界面更新能力通过体验主机提供给超市。
- [ ] 超市改用共享输入方向、反向锁定、动作节流和全局控制区。
- [ ] 删除超市自定义顶部栏、警戒、出口与控制区结构和样式。
- [ ] 运行定向测试并确认通过。

### 任务四：清理无效场景物件

**文件：**
- 修改：`src/levels/supermarket/world.js`
- 测试：`tests/levels/supermarketWorld.test.js`

**接口：**
- 世界继续输出场景、相机、人物、障碍物、互动点和视线判断。
- 世界不再输出出口状态接口。

- [ ] 增加失败测试，证明场景没有四条白色灯具、悬浮灯牌和红色出口条。
- [ ] 运行测试并确认因旧物件仍存在而失败。
- [ ] 删除灯具模型与完整出口资源，保留环境光和方向光。
- [ ] 运行世界测试并确认通过。

### 任务五：完整验证与提交

**文件：**
- 更新：`docs/superpowers/specs/2026-07-29-超市取证玩法统一修订设计.md`
- 新增：`docs/superpowers/plans/2026-07-29-超市取证玩法统一修订实施计划.md`

- [ ] 运行全部自动化测试，确认零失败。
- [ ] 运行生产构建，确认构建成功且无错误。
- [ ] 在浏览器依次验证第一关和超市：移动手感、暂停位置、开局找自己、三秒情侣标记、四次有效拍照和立即通关。
- [ ] 检查页面不存在警戒、出口提示、四条白灯、悬浮灯牌和红色出口条。
- [ ] 检查版本差异，只包含本次任务文件和两份文档，不包含用户的 `package-lock.json`。
- [ ] 使用中文提交信息提交完整可用代码。
