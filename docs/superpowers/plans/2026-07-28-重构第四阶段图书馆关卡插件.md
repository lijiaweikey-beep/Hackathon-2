# 图书馆关卡插件 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将“图书馆”关卡的生成、情侣行为和双目标命中判定迁入独立插件。

**Architecture:** 关卡运行器把 `handleAction` 的返回值交还主循环，插件借此覆盖通用命中结果。情侣使用通用 `separationGroup` 标记声明“组内不互相推开”，避免通用碰撞系统识别具体关卡。

**Tech Stack:** JavaScript ES Modules、Three.js、Vite、Node.js 内置测试运行器

## Global Constraints

- 不新增运行时依赖。
- 图书馆插件不得导入 `src/game.js` 或访问 DOM。
- 主循环不得新增 `library` 类型判断。
- 旧关卡继续可玩。
- 每个提交使用中文提交信息，且提交时功能完整可运行。

---

### Task 1: 让关卡动作返回自定义结果

**Files:**
- Modify: `src/levels/levelRunner.js`
- Modify: `tests/levels/levelRunner.test.js`

**Interfaces:**
- Consumes: `instance.handleAction(action)`
- Produces: `runner.handleAction(action)` 返回实例结果；无活动关卡或异常时返回 `undefined`

- [x] **Step 1: 编写失败测试**

```js
test("关卡动作可以向主循环返回结果", () => {
  const runner = createLevelRunner({
    createContext: ({ scope }) => ({ scope }),
  });
  const definition = createDefinition("action", []);
  definition.createLevel = () => ({
    start() {},
    update() {},
    handleAction: (action) => ({ received: action.type }),
    dispose() {},
  });
  runner.load(definition);
  assert.deepEqual(runner.handleAction({ type: "inspect" }), { received: "inspect" });
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `node --test tests/levels/levelRunner.test.js`
Expected: FAIL，实际返回 `undefined`。

- [x] **Step 3: 返回实例动作结果**

```js
handleAction(action) {
  if (!active) return undefined;
  try {
    return active.instance.handleAction(action);
  } catch (error) {
    fail(error, active.definition);
    return undefined;
  }
}
```

- [x] **Step 4: 运行关卡运行器测试**

Run: `node --test tests/levels/levelRunner.test.js`
Expected: PASS。

- [x] **Step 5: 提交**

```bash
git add src/levels/levelRunner.js tests/levels/levelRunner.test.js
git commit -m "支持关卡动作返回自定义结果"
```

### Task 2: 提取图书馆插件

**Files:**
- Create: `src/levels/library/createLevel.js`
- Create: `tests/levels/libraryLevel.test.js`

**Interfaces:**
- Consumes: 角色创建、移动、朝向、口红标记、随机数、障碍检测和命中朝向判断能力
- Produces: `createLibraryLevel(context)`，通过 `handleAction({ type: "findHitTarget", playerPos, facing })` 返回情侣双目标

- [x] **Step 1: 编写失败测试**

```js
test("图书馆插件生成情侣和剩余路人", () => {
  const context = createFakeContext({ npcCount: 5 });
  const level = createLibraryLevel(context);
  level.start();
  assert.deepEqual(context.created.map(({ id }) => id), [0, 1, 2, 3, 4]);
  assert.equal(context.members[0].levelManaged, true);
  assert.equal(context.members[0].separationGroup, context.members[1].separationGroup);
});

test("面对情侣任意一人时返回双目标命中", () => {
  const context = createFakeContext({ npcCount: 2 });
  const level = createLibraryLevel(context);
  level.start();
  const hit = level.handleAction({
    type: "findHitTarget",
    playerPos: new THREE.Vector3(0, 0, 1),
    facing: new THREE.Vector2(0, -1),
  });
  assert.equal(hit.correct, true);
  assert.deepEqual(hit.npcs, context.members);
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `node --test tests/levels/libraryLevel.test.js`
Expected: FAIL，提示 `src/levels/library/createLevel.js` 不存在。

- [x] **Step 3: 实现图书馆插件**

```js
import * as THREE from "three";
import { HIT_PAIR_RANGE, NPC_SPEED } from "../../config/constants.js";

export function createLibraryLevel(context) {
  let pair = null;

  function start() {
    const a = context.createNpc(0, { lover: true });
    const b = context.createNpc(1, { lover: true });
    const separationGroup = Symbol("library-lovers");
    a.levelManaged = true;
    b.levelManaged = true;
    a.separationGroup = separationGroup;
    b.separationGroup = separationGroup;
    a.group.position.set(-0.38, 0, -0.2);
    b.group.position.set(0.38, 0, -0.2);
    context.addNpc(a);
    context.addNpc(b);
    pair = {
      members: [a, b],
      state: "kiss",
      timer: 2.2,
      meetingPoint: new THREE.Vector3(0, 0, -0.2),
      scatterPoints: [new THREE.Vector3(-3, 0, 2.5), new THREE.Vector3(3, 0, 1.6)],
    };
    for (let id = 2; id < context.npcCount; id += 1) context.addWanderNpc(id);
  }

  function randomMeetingPoint() {
    let point;
    let attempts = 0;
    do {
      point = new THREE.Vector3(
        context.randomRange(-5.5, 5.5),
        0,
        context.randomRange(-4.5, 5.8),
      );
      attempts += 1;
    } while (attempts < 30 && context.collidesWithObstacle(point));
    return point;
  }

  function update(deltaSeconds) {
    if (!pair) return;
    const [a, b] = pair.members;
    if (!a.alive || !b.alive) return;
    if (pair.state === "kiss") {
      a.walking = false;
      b.walking = false;
      pair.timer -= deltaSeconds;
      context.faceNpcToward(a, b.group.position);
      context.faceNpcToward(b, a.group.position);
      const intensity = Math.min(1, a.markIntensity + deltaSeconds * 0.32);
      context.setLipstick(a, intensity);
      context.setLipstick(b, intensity);
      if (pair.timer <= 0) {
        pair.state = "scatter";
        pair.timer = context.randomRange(3.4, 4.8);
        const angle = context.randomRange(0, Math.PI * 2);
        pair.scatterPoints = [
          new THREE.Vector3(
            Math.cos(angle) * context.randomRange(3.2, 5.6),
            0,
            Math.sin(angle) * context.randomRange(2.8, 5.4),
          ),
          new THREE.Vector3(
            Math.cos(angle + Math.PI) * context.randomRange(3.2, 5.6),
            0,
            Math.sin(angle + Math.PI) * context.randomRange(2.8, 5.4),
          ),
        ];
      }
      return;
    }
    if (pair.state === "scatter") {
      a.walking = true;
      b.walking = true;
      const aDone = context.moveNpcToward(a, pair.scatterPoints[0], NPC_SPEED * 1.15, deltaSeconds);
      const bDone = context.moveNpcToward(b, pair.scatterPoints[1], NPC_SPEED * 1.15, deltaSeconds);
      pair.timer -= deltaSeconds;
      if ((aDone && bDone) || pair.timer <= 0) {
        pair.meetingPoint = randomMeetingPoint();
        pair.state = "approach";
      }
      return;
    }
    if (pair.state === "approach") {
      const offset = new THREE.Vector3(0.32, 0, 0);
      a.walking = true;
      b.walking = true;
      const aDone = context.moveNpcToward(
        a,
        pair.meetingPoint.clone().sub(offset),
        NPC_SPEED * 1.05,
        deltaSeconds,
      );
      const bDone = context.moveNpcToward(
        b,
        pair.meetingPoint.clone().add(offset),
        NPC_SPEED * 1.05,
        deltaSeconds,
      );
      if (aDone && bDone) {
        pair.state = "kiss";
        pair.timer = context.randomRange(1.7, 2.6);
      }
    }
  }

  function findHitTarget({ playerPos, facing }) {
    if (!pair) return undefined;
    const [a, b] = pair.members;
    if (!a.alive || !b.alive) return undefined;
    const toA = new THREE.Vector2(
      a.group.position.x - playerPos.x,
      a.group.position.z - playerPos.z,
    );
    const toB = new THREE.Vector2(
      b.group.position.x - playerPos.x,
      b.group.position.z - playerPos.z,
    );
    const aInRange = toA.length() <= HIT_PAIR_RANGE && context.isFacingTarget(facing, toA);
    const bInRange = toB.length() <= HIT_PAIR_RANGE && context.isFacingTarget(facing, toB);
    return aInRange || bInRange ? { correct: true, npcs: [a, b] } : undefined;
  }

  return {
    start,
    update,
    handleAction(action) {
      return action.type === "findHitTarget" ? findHitTarget(action) : undefined;
    },
    dispose() {
      pair = null;
    },
  };
}
```

- [x] **Step 4: 运行插件测试**

Run: `node --test tests/levels/libraryLevel.test.js`
Expected: PASS。

- [x] **Step 5: 提交**

```bash
git add src/levels/library/createLevel.js tests/levels/libraryLevel.test.js
git commit -m "提取图书馆关卡插件"
```

### Task 3: 接入图书馆插件

**Files:**
- Modify: `src/levels/library/definition.js`
- Create: `tests/levels/libraryDefinition.test.js`
- Modify: `src/game.js`

**Interfaces:**
- Consumes: `levelRunner.handleAction()`、`npc.separationGroup`
- Produces: 图书馆关卡通过通用插件路径运行

- [x] **Step 1: 编写关卡描述失败测试**

```js
test("图书馆关卡使用插件生命周期", () => {
  assert.equal(libraryDefinition.legacy, false);
  assert.equal(typeof libraryDefinition.createLevel, "function");
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `node --test tests/levels/libraryDefinition.test.js`
Expected: FAIL，当前描述仍为兼容关卡。

- [x] **Step 3: 绑定插件并补充上下文**

关卡描述设置 `legacy: false`、`createLevel: createLibraryLevel`、`decoyCount: 4`；运行时上下文增加 `setLipstick`、`collidesWithObstacle` 和 `isFacingTarget`。

- [x] **Step 4: 接入自定义命中和碰撞关系**

`findHitTarget()` 先请求 `levelRunner.handleAction({ type: "findHitTarget", playerPos, facing })`；通用分离逻辑遇到相同非空 `separationGroup` 时跳过。删除入口中的图书馆生成、更新、随机会合点和双目标判断分支。

- [x] **Step 5: 运行全量测试与构建**

Run: `npm test && npm run build`
Expected: 全部测试通过，Vite 构建成功。

- [x] **Step 6: 浏览器烟雾验证**

Run: `npm run dev -- --host 127.0.0.1`
Expected: 图书馆简报和三维场景正常，计时持续更新；凌晨三点插件和两个旧关卡入口正常。

- [x] **Step 7: 提交**

```bash
git add src/game.js src/levels/library/definition.js tests/levels/libraryDefinition.test.js
git commit -m "接入图书馆关卡插件生命周期"
```

### Task 4: 完成第四阶段验收

**Files:**
- Modify: `docs/superpowers/plans/2026-07-28-重构第四阶段图书馆关卡插件.md`

**Interfaces:**
- Consumes: Task 1 至 Task 3 的测试与提交
- Produces: 支持自定义命中判定的第二个可复制插件样例

- [x] **Step 1: 检查入口残留**

Run: `rg -n '=== "library"|updateLovers|randomMeetingPoint|levelState\\.pair' src/game.js`
Expected: 无匹配。

- [x] **Step 2: 运行最终验证**

Run: `npm test && npm run build && git status --short`
Expected: 测试和构建通过；状态中只剩任务开始前的无关用户文件。

- [x] **Step 3: 勾选步骤并提交**

```bash
git add docs/superpowers/plans/2026-07-28-重构第四阶段图书馆关卡插件.md
git commit -m "完成图书馆关卡插件迁移"
```
