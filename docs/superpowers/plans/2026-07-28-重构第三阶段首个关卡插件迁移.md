# 首个关卡插件迁移 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将“凌晨三点”从 `game.js` 的关卡类型分支迁入独立插件，并接通已实现的关卡运行器。

**Architecture:** 关卡插件只通过运行时上下文使用生成角色、移动、线索标记和环境更新能力，不直接读写入口文件变量。`game.js` 保留通用循环和旧关卡兼容路径；已迁移关卡由 `levelRunner` 统一加载、更新和销毁。

**Tech Stack:** JavaScript ES Modules、Three.js、Vite、Node.js 内置测试运行器

## Global Constraints

- 不新增运行时依赖。
- 新插件不得导入 `src/game.js`。
- 新插件不得直接访问 DOM、全局场景或其他关卡实现。
- 旧关卡继续可玩，迁移期间保持渐进兼容。
- 每个提交使用中文提交信息，且提交时功能完整可运行。

---

### Task 1: 提取“凌晨三点”关卡插件

**Files:**
- Create: `src/levels/gaming/createLevel.js`
- Create: `tests/levels/gamingLevel.test.js`

**Interfaces:**
- Consumes: `context.createNpc`、`context.addNpc`、`context.addWanderNpc`、`context.computers`、`context.npcCount`、`context.randomRange`、`context.randomOpenPosition`、`context.faceNpcToward`、`context.moveNpcToward`、`context.setBlackEye`、`context.updateEnvironment`
- Produces: `createGamingLevel(context)`，返回 `start()`、`update(deltaSeconds)`、`handleAction(action)`、`dispose()`

- [ ] **Step 1: 编写失败测试**

```js
test("凌晨三点插件生成目标和剩余路人", () => {
  const context = createFakeContext({ npcCount: 4 });
  const level = createGamingLevel(context);
  level.start();

  assert.deepEqual(context.created.map(({ id }) => id), [0, 1, 2, 3]);
  assert.equal(context.created[0].flags.gamingTarget, true);
  assert.equal(context.target.levelManaged, true);
  assert.equal(context.target.markIntensity, 0.62);
});

test("凌晨三点目标结束打游戏后离开电脑", () => {
  const context = createFakeContext({ npcCount: 2 });
  const level = createGamingLevel(context);
  level.start();
  level.update(3);

  assert.equal(context.target.script.state, "leave");
  assert.equal(context.target.markIntensity, 1);
  assert.equal(context.environmentUpdates, 1);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/levels/gamingLevel.test.js`
Expected: FAIL，提示 `src/levels/gaming/createLevel.js` 不存在。

- [ ] **Step 3: 实现最小插件**

```js
export function createGamingLevel(context) {
  let target = null;

  function start() {
    target = context.createNpc(0, { gamingTarget: true });
    target.levelManaged = true;
    target.group.position.copy(context.computers[2]);
    target.group.position.x += 0.2;
    target.script = {
      state: "play",
      timer: 2.6,
      playDuration: 2.6,
      computerIndex: 2,
      waypoint: null,
    };
    context.faceNpcToward(target, context.computers[2].clone().setZ(context.computers[2].z - 1.2));
    context.setBlackEye(target, 0.62);
    context.addNpc(target);
    for (let id = 1; id < context.npcCount; id += 1) context.addWanderNpc(id);
  }

  function update(deltaSeconds) {
    context.updateEnvironment(deltaSeconds);
    if (!target?.alive) return;
    const script = target.script;
    if (script.state === "play") {
      target.walking = false;
      script.timer -= deltaSeconds;
      const computer = context.computers[script.computerIndex];
      const facingPoint = computer.clone();
      facingPoint.z += computer.z > 0 ? -1.1 : 1.1;
      context.faceNpcToward(target, facingPoint);
      const progress = 1 - script.timer / (script.playDuration || script.timer || 1);
      context.setBlackEye(target, 0.62 + progress * 0.28);
      if (script.timer <= 0) {
        context.setBlackEye(target, 1);
        script.state = "leave";
        script.timer = context.randomRange(5, 7);
        script.waypoint = context.randomOpenPosition();
      }
      return;
    }
    if (script.state === "leave") {
      target.walking = true;
      const reached = context.moveNpcToward(
        target,
        script.waypoint,
        context.npcSpeed * 1.08,
        deltaSeconds,
      );
      script.timer -= deltaSeconds;
      if (reached || script.timer <= 0) {
        script.computerIndex = Math.floor(context.randomRange(0, context.computers.length));
        script.waypoint = context.computers[script.computerIndex].clone();
        script.state = "seek";
      }
      return;
    }
    if (script.state === "seek") {
      target.walking = true;
      const reached = context.moveNpcToward(
        target,
        script.waypoint,
        context.npcSpeed * 1.12,
        deltaSeconds,
      );
      if (reached) {
        script.state = "play";
        script.timer = context.randomRange(2.2, 3.4);
        script.playDuration = script.timer;
        context.setBlackEye(target, 0.62);
      }
    }
  }

  return {
    start,
    update,
    handleAction() {},
    dispose() {
      target = null;
    },
  };
}
```

- [ ] **Step 4: 运行插件测试**

Run: `node --test tests/levels/gamingLevel.test.js`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/levels/gaming/createLevel.js tests/levels/gamingLevel.test.js
git commit -m "提取凌晨三点关卡插件"
```

### Task 2: 接入关卡运行器

**Files:**
- Modify: `src/levels/gaming/definition.js`
- Create: `tests/levels/gamingDefinition.test.js`
- Modify: `src/game.js`

**Interfaces:**
- Consumes: `createLevelRunner({ createContext, onError })`、`createGamingLevel(context)`
- Produces: 通用插件上下文；非兼容关卡通过 `levelRunner.load/start/update/dispose` 运行

- [ ] **Step 1: 编写关卡描述失败测试**

```js
test("凌晨三点关卡使用插件生命周期", () => {
  assert.equal(gamingDefinition.legacy, false);
  assert.equal(typeof gamingDefinition.createLevel, "function");
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/levels/gamingDefinition.test.js`
Expected: FAIL，当前描述仍为 `legacy: true` 且没有 `createLevel`。

- [ ] **Step 3: 将插件绑定到关卡描述**

```js
import { createGamingLevel } from "./createLevel.js";

export default {
  id: "gaming",
  legacy: false,
  createLevel: createGamingLevel,
  decoyCount: 3,
};
```

保留现有关卡的标题、任务、线索和结算文案。

- [ ] **Step 4: 运行关卡描述测试**

Run: `node --test tests/levels/gamingDefinition.test.js`
Expected: PASS。

- [ ] **Step 5: 建立最小运行时上下文**

```js
const levelRunner = createLevelRunner({
  createContext: ({ scope }) => ({
    scope,
    npcCount: getMatchNpcCount(),
    computers: levelState.computers,
    npcSpeed: NPC_SPEED,
    createNpc,
    addNpc(npc) {
      npcs.push(npc);
      scene.add(npc.group);
    },
    addWanderNpc,
    randomRange,
    randomOpenPosition,
    faceNpcToward,
    moveNpcToward,
    setBlackEye,
    updateEnvironment: updateFlashlight,
  }),
  onError: (error, definition) => {
    console.error(`关卡运行失败：${definition.id}`, error);
  },
});
```

- [ ] **Step 6: 接通加载、更新和销毁**

在 `spawnNpcs(level)` 中，非兼容关卡执行 `load` 和 `start`；在 `updateNpcs(dt)` 中执行 `update`，并让通用循环只更新未标记 `levelManaged` 的路人；在 `disposeScene()` 中调用 `dispose`。删除原来的 `gaming` 生成分支、`updateGamingTarget` 和按关卡标识更新手电筒的判断。

- [ ] **Step 7: 运行全量测试与构建**

Run: `npm test && npm run build`
Expected: 全部测试通过，Vite 构建成功。

- [ ] **Step 8: 浏览器烟雾验证**

Run: `npm run dev -- --host 127.0.0.1`
Expected: 点击“凌晨三点”可打开简报并进入游戏；页面控制台无运行错误；另外三个兼容关卡仍可打开简报。

- [ ] **Step 9: 提交**

```bash
git add src/game.js src/levels/gaming/definition.js tests/levels/gamingDefinition.test.js
git commit -m "接入首个关卡插件生命周期"
```

### Task 3: 完成第三阶段验收

**Files:**
- Modify: `docs/superpowers/plans/2026-07-28-重构第三阶段首个关卡插件迁移.md`

**Interfaces:**
- Consumes: Task 1 和 Task 2 的测试与提交
- Produces: 首个可复制关卡插件示例和验收记录

- [ ] **Step 1: 检查入口残留**

Run: `rg -n '=== "gaming"|updateGamingTarget' src/game.js`
Expected: 无匹配。

- [ ] **Step 2: 运行最终验证**

Run: `npm test && npm run build && git status --short`
Expected: 测试和构建通过；状态中只剩任务开始前的无关用户文件。

- [ ] **Step 3: 勾选已完成步骤并提交**

```bash
git add docs/superpowers/plans/2026-07-28-重构第三阶段首个关卡插件迁移.md
git commit -m "完成首个关卡插件迁移"
```
