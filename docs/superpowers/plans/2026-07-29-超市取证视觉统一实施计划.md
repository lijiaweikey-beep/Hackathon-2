# 超市取证视觉统一实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **面向执行者：** 必须使用对应的计划执行技能逐项实施，并用复选框跟踪进度。

**Goal:** 保留“超市取证”的全部独立玩法规则，把人物、场景、镜头、灯光和界面统一为前面主线关卡的低多边形风格。

**Architecture:** 新增人物模块和场景模块，分别负责低多边形角色与超市世界；原体验模块只编排输入、移动、取证、警戒和通关。视觉层只接受出口开关状态，不读取或修改规则对象。

**Tech Stack:** 原生 JavaScript、Three.js、CSS、Node.js 内置测试运行器。

## Global Constraints

- 保持 90 秒、4 张照片、5～6 次机会、有效拍摄距离、警戒增长和衰减速度不变。
- 六组货架的碰撞坐标和尺寸不变。
- 超市模块不得导入其他关卡目录，只复用共享实体模块。
- 不新增第三方依赖。
- 手机端强制横屏策略沿用现有全局实现，关卡界面需适配横屏高度不超过 480 像素。
- 用户已有的锁文件改动不纳入本任务提交。

---

### Task 1: 低多边形人物与超市场景

**Files:**
- Create: `src/levels/supermarket/actors.js`
- Create: `src/levels/supermarket/world.js`
- Create: `tests/levels/supermarketActors.test.js`
- Create: `tests/levels/supermarketWorld.test.js`

**Interfaces:**
- Consumes: `createLowPolyPerson(palette)`、`LOW_POLY_NPC_PALETTES`、`host.rendering.createScene()`、`host.rendering.createCamera(options)`。
- Produces: `createSupermarketPerson(palette, x, z)`，返回低多边形人物根节点。
- Produces: `createSupermarketCast(scene)`，返回 `{ player, couple, customers }`。
- Produces: `createSupermarketWorld(host)`，返回 `{ scene, camera, shelves, products, checkout, exit, player, couple, customers, setExitOpen }`。

- [ ] **Step 1: 编写人物失败测试**

新增真实人物结构测试，明确它捕获“重新退回胶囊人物”这一回归：

```js
import assert from "node:assert/strict";
import test from "node:test";
import * as THREE from "three";
import {
  createSupermarketCast,
  createSupermarketPerson,
  SUPERMARKET_PLAYER_PALETTE,
} from "../../src/levels/supermarket/actors.js";

test("超市人物使用共享低多边形身体结构", () => {
  const person = createSupermarketPerson(SUPERMARKET_PLAYER_PALETTE, 2, 3);
  assert.equal(person.position.x, 2);
  assert.equal(person.position.z, 3);
  assert.ok(person.userData.visual instanceof THREE.Group);
  assert.ok(person.userData.leftArm instanceof THREE.Group);
  assert.ok(person.userData.rightLeg instanceof THREE.Group);
});

test("超市人物阵容包含主角、目标情侣和十四名顾客", () => {
  const scene = new THREE.Scene();
  const cast = createSupermarketCast(scene);
  assert.equal(cast.couple.length, 2);
  assert.equal(cast.customers.length, 14);
  assert.equal(scene.children.includes(cast.player), true);
  assert.notEqual(
    cast.couple[0].userData.colors[0],
    cast.couple[1].userData.colors[0],
  );
});
```

- [ ] **Step 2: 运行人物测试并确认失败**

Run: `node --test tests/levels/supermarketActors.test.js`

Expected: FAIL，原因是 `src/levels/supermarket/actors.js` 尚不存在。

- [ ] **Step 3: 实现最小低多边形人物模块**

在 `actors.js` 中复用共享人物工厂，不复制人物网格：

```js
import { createLowPolyPerson } from "../../entities/lowPolyPerson.js";
import { LOW_POLY_NPC_PALETTES } from "../../entities/palettes.js";

export const SUPERMARKET_PLAYER_PALETTE = {
  jacket: 0xf97316,
  jacketDark: 0xc2410c,
  shorts: 0x334155,
  shortsDark: 0x1e293b,
  cap: 0x0f766e,
  capAccent: 0xfacc15,
  sock: 0xbae6fd,
};

export function createSupermarketPerson(palette, x, z) {
  const { group } = createLowPolyPerson(palette);
  group.position.set(x, 0, z);
  group.scale.setScalar(0.86);
  return group;
}

export function createSupermarketCast(scene) {
  const customers = Array.from({ length: 14 }, (_, index) => {
    const x = -9 + (index % 7) * 3;
    const z = index < 7 ? -5.4 : 5.2;
    return createSupermarketPerson(
      LOW_POLY_NPC_PALETTES[index % LOW_POLY_NPC_PALETTES.length],
      x,
      z,
    );
  });
  const player = createSupermarketPerson(SUPERMARKET_PLAYER_PALETTE, 0, 6.2);
  const couple = [
    createSupermarketPerson(LOW_POLY_NPC_PALETTES[1], -1.2, -5.4),
    createSupermarketPerson(LOW_POLY_NPC_PALETTES[0], 1.2, -5.4),
  ];
  scene.add(...customers, player, ...couple);
  return { player, couple, customers };
}
```

- [ ] **Step 4: 运行人物测试并确认通过**

Run: `node --test tests/levels/supermarketActors.test.js`

Expected: PASS，2 项测试通过。

- [ ] **Step 5: 编写场景失败测试**

新增真实 Three.js 场景测试，明确它捕获“货架退回单色方块、缺少商品、出口状态不更新”三类回归：

```js
import assert from "node:assert/strict";
import test from "node:test";
import * as THREE from "three";
import { createSupermarketWorld } from "../../src/levels/supermarket/world.js";

function createHost() {
  return {
    rendering: {
      THREE,
      createScene: () => new THREE.Scene(),
      createCamera: (options) => {
        const camera = new THREE.OrthographicCamera(
          options.left,
          options.right,
          options.top,
          options.bottom,
          0.1,
          100,
        );
        camera.position.set(...options.position);
        return camera;
      },
    },
  };
}

test("超市场景提供完整货架、商品、人物和收银区", () => {
  const world = createSupermarketWorld(createHost());
  assert.equal(world.shelves.length, 6);
  assert.ok(world.products.length >= 72);
  assert.equal(world.customers.length, 14);
  assert.equal(world.couple.length, 2);
  assert.ok(world.checkout instanceof THREE.Group);
  assert.ok(world.exit instanceof THREE.Mesh);
});

test("出口开放时灯光和通道同时切换为绿色", () => {
  const world = createSupermarketWorld(createHost());
  world.setExitOpen(true);
  assert.equal(world.exit.material.color.getHex(), 0x22c55e);
  assert.equal(world.exit.userData.statusLight.color.getHex(), 0x22c55e);
});
```

- [ ] **Step 6: 运行场景测试并确认失败**

Run: `node --test tests/levels/supermarketWorld.test.js`

Expected: FAIL，原因是 `src/levels/supermarket/world.js` 尚不存在。

- [ ] **Step 7: 实现低多边形超市场景**

在 `world.js` 中：

- 用米灰地面、暖白三面墙、规则灯带创建室内空间。
- 保留以下碰撞数据：

```js
const SHELVES = [
  [-6.2, -2.5, 2.4, 0.65],
  [0, -2.5, 2.4, 0.65],
  [6.2, -2.5, 2.4, 0.65],
  [-6.2, 2.2, 2.4, 0.65],
  [0, 2.2, 2.4, 0.65],
  [6.2, 2.2, 2.4, 0.65],
];
```

- 每个货架由两根立柱、三层层板和至少十二件商品组成，商品使用红、黄、青、蓝四种低多边形材质循环。
- 在 `z = 7.2` 创建收银台、出口通道和状态灯。
- 镜头继续覆盖 `24 × 15` 的活动区，位置使用 `[0, 18, 13]`。
- `setExitOpen(open)` 同步设置通道材质与状态灯颜色，关闭为 `0xdc2626`，开放为 `0x22c55e`。
- 调用 `createSupermarketCast(scene)` 加入主角、情侣和顾客。

- [ ] **Step 8: 运行人物与场景测试并确认通过**

Run: `node --test tests/levels/supermarketActors.test.js tests/levels/supermarketWorld.test.js`

Expected: PASS，4 项测试通过。

### Task 2: 接入玩法循环并统一界面

**Files:**
- Modify: `src/levels/supermarket/createExperience.js`
- Modify: `src/levels/supermarket/styles.css`
- Modify: `tests/levels/supermarketLevel.test.js`

**Interfaces:**
- Consumes: `createSupermarketWorld(host)` 及其返回值。
- Produces: 不改变 `createSupermarketExperience(host)` 的公开生命周期接口。

- [ ] **Step 1: 扩展生命周期失败测试**

在原生命周期测试中记录创建世界后可观察的渲染与释放行为，并断言挂载内容包含统一信息层语义：

```js
assert.match(
  calls.find(([name]) => name === "content")[1],
  /取证进度/,
);
assert.match(
  calls.find(([name]) => name === "content")[1],
  /警戒/,
);
```

该测试捕获“新界面没有明确呈现取证和警戒信息”的回归。

- [ ] **Step 2: 运行生命周期测试并确认失败**

Run: `node --test tests/levels/supermarketLevel.test.js`

Expected: FAIL，当前挂载内容只有“取证”，没有“取证进度”和“警戒”标签。

- [ ] **Step 3: 将体验模块接入新世界**

修改 `createExperience.js`：

```js
import { createPhotoEvidenceRules } from "./rules.js";
import { createSupermarketWorld } from "./world.js";
```

- 删除本地 `addBox`、`createPerson` 和 `buildWorld`。
- 用单一 `world` 变量保存 `createSupermarketWorld(host)` 的返回值。
- 碰撞和遮挡继续读取 `world.shelves`。
- 移动和情侣巡逻继续更新 `world.player.position`、`world.couple[n].position`。
- `updateHud()` 调用 `world.setExitOpen(state.exitOpen)`，不直接修改材质。
- 渲染和释放使用 `world.scene`、`world.camera`，保持生命周期行为不变。
- 挂载信息改为“取证进度”和“警戒”，按钮及数据属性保持原值，避免输入行为回归。

- [ ] **Step 4: 更新界面样式**

修改 `styles.css`：

- 信息条改为深蓝黑半透明玻璃层，使用 `rgba(7, 18, 28, .82)`、`1px` 浅色边框、`10px` 圆角和轻微阴影。
- 警戒轨道直接包含“警戒”标签，出口提示独立居中。
- 摇杆底盘与按钮使用同一深色玻璃语言；摇杆旋钮使用 `#38d6b1`。
- 拍照准备态使用青绿色描边和脉冲，保留原按钮尺寸及点击范围。
- 简报和结算面板使用暖白标题、灰蓝正文、青绿色主按钮。
- 在横屏且高度不超过 `480px` 时缩小信息条、面板、摇杆和拍照按钮的视觉尺寸，但触控区不小于 `72px`。

- [ ] **Step 5: 运行超市相关测试并确认通过**

Run: `node --test tests/levels/supermarketLevel.test.js tests/levels/supermarketRules.test.js tests/levels/supermarketActors.test.js tests/levels/supermarketWorld.test.js`

Expected: PASS，生命周期与规则保持正常，新人物和世界结构通过。

### Task 3: 架构、全量与手机横屏验收

**Files:**
- Modify only if verification exposes a defect: `src/levels/supermarket/actors.js`
- Modify only if verification exposes a defect: `src/levels/supermarket/world.js`
- Modify only if verification exposes a defect: `src/levels/supermarket/createExperience.js`
- Modify only if verification exposes a defect: `src/levels/supermarket/styles.css`

**Interfaces:**
- Consumes: 完整超市关卡和现有应用入口。
- Produces: 可构建、可在桌面与手机横屏游玩的 25 岁主线关卡。

- [ ] **Step 1: 运行架构测试**

Run: `node --test tests/architecture/moduleBoundaries.test.js`

Expected: PASS，超市只依赖共享实体和本关卡模块。

- [ ] **Step 2: 运行全量自动测试**

Run: `npm test`

Expected: PASS，无失败或未处理错误。

- [ ] **Step 3: 运行生产构建**

Run: `npm run build`

Expected: PASS，生成生产资源。

- [ ] **Step 4: 浏览器桌面验收**

启动本地开发服务并直接进入 25 岁“超市取证”：

- 时间线卡片可直接进入。
- 简报面板不遮挡标题和开始按钮。
- 开始后可见低多边形主角、情侣、十四名顾客、六组商品货架和收银出口。
- 键盘移动、碰撞、拍照准备态、白色闪光和警戒条正常。
- 四张照片后出口提示和收银灯变绿。

- [ ] **Step 5: 浏览器手机横屏验收**

视口使用 `844 × 390`：

- 不出现强制旋转遮罩。
- 顶部取证、时间、警戒与出口提示完整可见。
- 左侧摇杆和右侧拍照按钮不被安全区或边缘裁切。
- 简报开始按钮、结算返回按钮完整可点击。
- 控制台无错误。

- [ ] **Step 6: 检查差异并提交**

Run: `git diff --check`

Expected: 无输出。

只暂存本计划涉及的超市源码和测试，排除用户原有锁文件改动。

Run: `git commit -m "统一超市取证关卡视觉风格"`

Expected: 提交成功，工作区仅保留用户原有改动。
